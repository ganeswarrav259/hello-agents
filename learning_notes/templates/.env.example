# 仅用于说明字段；本目录模板不会被课程自动加载。
# 在实际案例说明要求的位置复制为 .env，并只在本机填写。
LLM_MODEL_ID=replace_with_your_model_id
LLM_API_KEY=replace_with_your_api_key
LLM_BASE_URL=replace_with_your_trusted_service_url
LLM_TIMEOUT=60
